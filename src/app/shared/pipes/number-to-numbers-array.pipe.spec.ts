import { NumberToNumbersArrayPipe } from './number-to-numbers-array.pipe';

describe('NumberToNumbersArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToNumbersArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
